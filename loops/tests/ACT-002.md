# ACT-002

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。時計を 2026-10-05 正午(UTC)に固定し、`insert`(activities)で日付を省くと 400 にならず `occurred_on` に今日が入ること(返り値と保存された行の両方)、送った日付は既定値より勝つこと、`occurred_on: null` を明示すれば必須の検証で 400 になり行が増えないことを確かめる。`insert` の既定値の行を外したときと、期待値を変えたときに落ちることを確認済み
