# IO-065

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` に追記。`accounts` を target にした参照の項目で `coerce` を呼び、表示名が 1 件だけ一致すれば(前後の空白があっても)その ID を返すこと、無い名前は「見つかりません」で投げることを確かめる。次に同名の行を 1 件足し、同じ名前では「2 件あります。ID で指定してください」で投げること(どちらも選ばない)、UUID なら名前が重なっていてもその行の ID をそのまま返すことを確かめる
  - 期待値の確認: `csv.ts` で、同名 2 件以上の throw を無効にする・UUID の分岐を無効にする・表示名の比較を常に外す、のどれでも落ちる。確認して戻した
