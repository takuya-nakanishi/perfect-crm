# IO-028

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。選択肢(見込み・商談中・受注・失注)の項目を持つ試しのテーブルに、定義順とばらばらの順で 15 行(見込み 2・商談中 1・受注 4・失注 3・空 5)を入れ、`count` を `group_by` で分ける。`order` 省略時は選択肢の定義順で、ラベルと色は選択肢のもの、空のグループは件数が最大でも `key: null`・「未設定」で末尾に出ること、`order: value_desc` では値の大きい順(受注 → 失注 → 見込み → 商談中)で、空のグループはやはり末尾に出ることを確かめる。value_desc を無視する・定義順で空を先頭にする・値の大きい順で空を末尾に回さない・空のラベルを「未設定」にしないの 4 通りで落ちることを確認済み
