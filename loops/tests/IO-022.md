# IO-022

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`query` に `limit: 0` を渡すと `records` が空、`references` も空で、`total` は全件数(絞り込みがあれば絞り込み後の件数)になることを確かめる。`limit: 3` と `offset` を 3 ずつ進めて取ると、どのページも `total` は全件数で、つなげると全件の並びと一致すること、`limit` を省くと `offset` から最後まで、末尾を越えた `offset` は空になることを確かめる。`limit` の判定を `!params.limit`(0 を省略扱い)にする変更と、`total` を返した件数に寄せる変更のどちらでも落ちることを確認済み
