# TASK-062

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。タスクの labels に `["follow_up","sales"]` を `update` すると定義順の `["sales","follow_up"]` で保存され、重複を含めて送っても 1 つずつになること、選択肢に無い値(`vip`)は 400 で保存済みの値が変わらないこと、`[]` は null になることを確かめる。製品側で並べ替えをやめる(送った順のまま保存する)と落ちること、空の配列を `"[]"` のまま保存すると落ちることを確認済み(戻してある)
