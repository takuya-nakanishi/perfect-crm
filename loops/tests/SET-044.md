# SET-044

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者で、金額(currency)・期日(date)・選択肢(select、ラベル「高い」→ 値 `hot`)を持つテーブルを作り、その 4 項目を受けるフォームを作る。ログアウトして `submitWebForm` に全部を文字で送り(`"1200000"`、`2026/9/30`、`高い`)、できたレコードが `1200000`・`2026-09-30`・`hot` になっていることを確かめる。直せない文字(`百万`、`9月30日`、`普通`)はそれぞれ 400。ログインし直して、保存された値・レコードが 1 件だけであること・`submissions` が 1 のままであることを確かめる。`submitForm` の `coerce` を外すと、400 への変換を外すと、それぞれ落ちることを確認済み
