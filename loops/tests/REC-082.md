# REC-082

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に REC-082 を書いた。`insert` で、参照の列(商談の `account_id`・`primary_contact_id`、タスクの `contact_id`)に無い ID や別テーブルの ID、利用者の列(`owner_id`・`assignee_id`)に無い ID やレコードの ID、チェックの列(`repeat_from_completion`)に文字 `"true"` や数値 `1` を渡すと 400 で行が増えないこと、有る ID と真偽なら作れることを確かめた。`validate` の relation・user・checkbox の各判定をそれぞれ外すと落ちることを確かめてから戻した
