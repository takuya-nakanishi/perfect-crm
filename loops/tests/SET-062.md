# SET-062

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加(SET-061 と同じ describe)。ドライブ型の項目を持つテーブルでレコードを 1 件作り、`createDriveDocument` にテキスト型の項目・無い項目を渡すと 400、無いレコードの ID を渡すと 404 になること、どれもドライブのファイルを増やさずレコードも変えないことを確かめる。型の検査を外す・404 を 400 にする、の 2 通りの壊し方でそれぞれ落ちることを確認済み
