# META-024

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。サイドバーに出さないテーブル(`hidden_a`・`hidden_b`)を足したうえで、サイドバーの先頭 3 件を逆順にして `reorderObjects` に渡すと、その 3 件が先頭、残り(出していないテーブルを含む)が元の順で後ろに続き、`position` が 1 から詰めて振り直されることを `getMeta()` で確かめる。無いテーブル・削除したテーブルを含めると 400 で、並びは変わらない。
  - `mocks/schema.ts` の `reorderObjects` で、残りの並べ方を逆順にすると落ち、無いテーブルの検査を外すと落ちることを確認済み
  - 注: `docs/design/04` §2 の `PUT /meta/objects/order` は「いまあるテーブル全部を順に」と書くが、モック(振る舞いの正)と画面(サイドバーは出しているテーブルだけを送る)は一部だけ渡すのを受け付ける。ケースの文はモックに沿うので、そのまま書いた。04 の文言を揃えるかは人が判断する
