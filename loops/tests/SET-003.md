# SET-003

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者でない利用者(fixtures の Misaki)でログインし、`listMcpTokens` / `createMcpToken` / `listWebForms` / `createWebForm` がそれぞれ 403 を返し、トークンとフォームが増えないことを確かめる。対照として管理者なら 4 つとも通ることも見る。`createWebForm` だけ、`listMcpTokens` だけ `requireAdmin` を外すとそれぞれ落ちることを確認済み
