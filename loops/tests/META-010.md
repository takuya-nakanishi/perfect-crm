# META-010

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。参照(`relation`)の項目の `target` が無い・空・存在しないテーブル・削除中のテーブル(作ってから `deleteObject` したもの)のどれでも `createObject` が 400 を返し、テーブルが作られないことを確かめる。生きているテーブル(`accounts`)を指せば作れて、`target` がそのまま残ることも確かめる。`mocks/schema.ts` の参照先の検査を「空でない」だけにした場合と、「削除中かを見ない」にした場合の両方で落ちることを確認済み
