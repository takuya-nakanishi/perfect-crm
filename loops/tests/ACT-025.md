# ACT-025

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。取引先を関連先にした done のタスク T を作り(取引先の時系列には完了として出ることを先に確かめる)、T の「前回」(`repeat_of`、relation)を T 自身にして自分を指させたうえで、関連先が T の活動と別のタスクの活動を入れ、`timeline('tasks', T)` が T を関連先にした活動だけを `kind: activity` で返し、T 自身の完了は出ないことを確かめる。`timeline` の `meta.key !== object` の判定を外すと T の完了が混ざって落ちることを確認済み(製品のコードは戻した)。関連先に `tasks` を持てないため、自分を指させるには `repeat_of` を使うしかなかった
- 備考: 実装は「開いたテーブルと同じテーブルの完了」を丸ごと除く。繰り返しの次回(`repeat_of` が T)が完了したとき T の時系列に出すかはケースの文が言っていないので、確かめていない
