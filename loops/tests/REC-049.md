# REC-049

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`find` は無い ID・別のテーブルの ID で null を返す(投げない)こと、商談の 1 件では `references` が取引先・主担当の責任者・担当者の 3 テーブルだけを持ち、各テーブルにその行が指す ID 1 件だけが参照先の表示名(name_field、利用者は名前)で入ること、タスクの関連先(polymorphic)が `related_object` のテーブルに `related_id` の表示名で入ることを確かめる。表示名は種データから直接引いて比べる。`references` を別の行から集める・polymorphic を集めない・表示名を id にする・無い ID で先頭の行を返す、のそれぞれで落ちることを確認済み(製品のコードは戻した)
