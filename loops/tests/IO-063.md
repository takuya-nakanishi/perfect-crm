# IO-063

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` に追記。取引先へ 5 行の CSV を取り込み、必須の取引先名が空(3 行目)・業種に無い選択肢「宇宙」(4 行目)・従業員数「たくさん」(5 行目)が `errors` に行番号(見出しを 1 行目と数える)と理由付きで残り、読める 2 行(丸山商事・森田物産)だけ作られることを確かめる。先に `dry_run` で同じ CSV を流し、`total`・`valid`・`errors` は本番と同じで `created_ids` が空、表の件数が変わらないことも確かめる
  - 期待値の確認: `csv.ts` の `if (!params.dry_run)` を常に真にする・必須の検査を外す・行番号を `i + 1` にする、のどれでも落ちる。確認して戻した
