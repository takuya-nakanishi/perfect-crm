# IO-082

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/richtext.test.ts` を新設。`sanitizeHtml` が `<script>` の要素を消し(中の文字は素の文字として残る)、`onclick` を落とし、`<img>`(`onerror` ごと)を落とし、`<div>`/`<span>` は中身だけ残し、`javascript:`(大文字・前の空白を含む)の `href` を落とし、`https:` の `href` に `target="_blank" rel="noreferrer"` を付けることを確かめる
  - 期待値の確認: `https:` の期待値から `rel="noreferrer"` を外すと落ちることを確認して戻した
