# META-058

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/viewModel.test.ts` に追記。選択肢は値ではなくラベル(複数は「, 」区切り)、利用者は ID ではなく名前で `$me` は「自分」、金額は `¥5,000,000`(演算子は「以上」)、日付のマクロ `$today+7` は「7 日後」、日付そのものは `2026/09/22` になることを確かめる。vitest の `describe` と名前がぶつかるため `describe as describeCondition` で取り込む
  - 期待値の確認: 「自分」を「私」に/選択肢のラベルを値に/金額の書式を外す/マクロのラベルを変える(製品側を一時的に変更)、の 4 通りでそれぞれ落ちることを確認して戻した
