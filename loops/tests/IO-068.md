# IO-068

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` に追記。タスク(状況・ラベル・取引先責任者・担当・資料)と活動(種別・記録者・書式付きの内容)を 1 件ずつ作って `exportCsv` で書き出し、先頭 3 バイトが BOM(EF BB BF)、見出しが項目名の並び、選択肢がラベル、複数選択が「営業、事務」、参照と利用者が表示名、ドライブが 1 ファイル 1 行の「名前 URL」、richtext がタグと文字参照を落とした文字であることを確かめる
  - `Blob.text()` は BOM を捨てるので、`arrayBuffer()` と `TextDecoder({ ignoreBOM: true })` で読む
  - 期待値の確認: `csv.ts` で BOM を外す・見出しを列名にする・richtext をそのまま出す・ドライブを URL だけにする、`engine.ts` の `displayValue` で複数選択を「,」でつなぐ・利用者と選択肢を値のまま出す、のどれでも落ちる。確認して戻した
