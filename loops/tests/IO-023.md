# IO-023

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。商談の名前(fixtures)で、`q` にひらがな(`くらうど`)・半角カタカナ(`ｸﾗｳﾄﾞ`)を渡すとカタカナの「配車管理のクラウド移行」に当たり、カタカナで打ったときと同じ結果になること、英字は大文字小文字・全角半角(`saas` / `SAAS` / `ｓａａｓ` / `ｗｉ－ｆｉ`)を問わず `SaaS`・`Wi-Fi` と同じ結果になること、当たらない語は空になることを確かめる。`normalizeText` の NFKC・`toLowerCase`・ひらがな→カタカナのどれを外しても落ちることを確認済み
