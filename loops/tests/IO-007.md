# IO-007

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/filter.test.ts` に追加。UTC 23:30 の日時の列を日付と `eq` で比べると、`ctx.timezone` が `Asia/Tokyo` なら翌日、`UTC` なら当日に一致すること、大小比較と `$today` も日付に直して比べることを確かめる。端末の時刻帯(`vi.stubEnv('TZ', …)`)を America/Los_Angeles・Asia/Tokyo・UTC に切り替えても結果が変わらないこと、`TZ=` を変えて Vitest を起動しても通ることを確認。`lib/filter.ts` の `align` で時刻帯を渡さないとき(端末の時刻帯になる)と、日付に直さないときに落ちることを確認済み
