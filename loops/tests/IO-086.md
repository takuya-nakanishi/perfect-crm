# IO-086

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/drive.test.ts` を新設。`parseDriveFiles` が配列を順に読み、`id` か `url` が無い・文字列でない要素と null を捨て、壊れた JSON・配列でない JSON・空文字・文字列でない値(null・undefined・数値)は空を返すこと、`driveKind` が Google のドキュメント / スプレッドシート / スライド・`application/pdf` をそれぞれの名前に、それ以外(`image/png`・空)を「ファイル」にすることを確かめる
  - 期待値の確認: 正しい配列の結果を 1 件に、PDF の種類を「ファイル」に、壊れた JSON の結果を 1 件に、混在の配列の結果を 1 件にすると落ちることを 1 つずつ確認して戻した
