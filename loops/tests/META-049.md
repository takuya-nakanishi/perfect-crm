# META-049

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。商談にカンバンのビューを作るとき、`group_by` に選択肢でない項目(文字・参照・金額・割合・日付・Google ドライブ・担当者・長文・日時の全部)や無い項目を指すと 400 で、ビューは増えないことを確かめる。一覧のビューを `updateView` でカンバンに変えるときも同じく 400 で、一覧のまま残ることも書いた。選択肢の項目なら作れる
  - 期待値の確認: `checkViewInput` の `g.type !== 'select'` の判定を外すと落ちることを確認して戻した
