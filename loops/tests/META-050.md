# META-050

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。取引先にビューを作ると `position` は取引先のビューの末尾 +1 で、続けて作るとさらに後ろ。商談に作ると商談の末尾から数え、取引先の並びは変わらない。`pin` を付けて作ると、渡した `pin.position`(1 や 0)は使われず、お気に入り全体(テーブルをまたぐ)の末尾 +1 になり、`label`・`show_count` はそのまま残ることを確かめる
  - 期待値の確認: `createView` の `position` を全テーブルの最大から数えるように変える/`pin.position` を振り直さない/同じテーブルのお気に入りだけから数える、の 3 通りでそれぞれ落ちることを確認して戻した
