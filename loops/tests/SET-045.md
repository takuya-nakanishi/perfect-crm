# SET-045

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者でテーブルとフォームを作り、受け付け中の送信が通ることを対照にして、`updateWebForm` で `enabled: false` にすると 404、戻すと通る。無い鍵と `rotateWebFormKey` で作り直す前の鍵は 404、新しい鍵は通る。`deleteObject` で先のテーブルを削除中にすると 404、`restoreObject` で戻すと通る。最後に、レコードが通った 4 件だけで、`submissions` も 4 であることを確かめる。`submitForm` の `enabled` の条件を外すと、削除中のテーブルの 404 を外すと、鍵が合わなくても受け付けるようにすると、それぞれ落ちることを確認済み
