# REC-083

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に REC-083 を書いた。`insert` で、選択肢に無い値(自作テーブルの select に無い値・表示名、取引先と活動の種別、タスクのラベル(multi_select)に無い値)は 400 で行が増えないこと、`max_length` 5 の text に 6 文字(英字・和文と補助漢字 `𠮷`)、活動の件名(200)に 201 文字、`max_length` 5 の richtext に書式を落として 6 文字の HTML は 400 で、ちょうど 5 文字(richtext は HTML としては長くても書式を落として 5 文字)・件名 200 文字なら作れることを確かめた。`validate` の「richtext は書式を落とす」「`>` を `>=` に」「符号位置で数える」「select の判定」「multi_select の判定」をそれぞれ壊すと落ちることを確かめてから戻した
