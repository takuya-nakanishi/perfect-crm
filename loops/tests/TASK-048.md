# TASK-048

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。repeat=weekly で期限が null(完了日から数える場合も含む)、期限はあるが repeat が null・空文字、の 4 通りを done にして、完了は効くが `repeat_of`=その id の次回ができず行数も変わらないことを確かめる。比べる相手として、両方あれば次回が 1 つできることも確かめる。期待値を反転させると落ちること、製品側の期限の判定を外すと落ちることを確認済み(repeat の空判定は外しても `nextDue` が null を返すので通る)
