# IO-004

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/filter.test.ts` に追加。`in` は配列の値のどれかと一致すれば真・どれとも違えば偽・NULL は偽、`not_in` は一致すれば偽・一致しなければ真、NULL(列が無い行も)は真であることを確かめる。期待値を反転させたときと、`lib/filter.ts` の `not_in` の否定を外したときに落ちることを確認済み
