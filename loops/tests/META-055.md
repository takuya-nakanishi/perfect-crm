# META-055

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/viewModel.test.ts` を新設。1 段の and/or → 条件の配列と join、1 条件だけの and → 書き戻すと単体の条件、入れ子(and の中の or)→ `advanced` に同じものを保つこと。往復で条件の集合・高度な条件の中身・join が保たれ、並びは「条件 → 高度な条件」に正規化され、2 回目の往復で形が変わらないことを確かめる
  - 注: 部分が 1 つだけのときは包みが取れるので、`{ or: [c] }` の join は往復で `and` になる。join の保存は部分が 2 つ以上のときに確かめている
  - 期待値の確認: 1 つだけのとき包みを取らない/`advanced` を捨てる/join を常に and にする(製品側を一時的に変更)、の 3 通りでそれぞれ落ちることを確認して戻した
