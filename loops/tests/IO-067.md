# IO-067

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` に追記。途中に読めない行を 1 つ挟んだ 5 行の CSV を `importCsv` で取り込み、`created_ids` が指すレコードの名前が CSV の並び(読めない行を除く)と同じ順であること、`query` の並べ替えなしの一覧と `created_at` 降順の一覧の上 4 件が `created_ids` と同じ順であることを確かめる
  - 期待値の確認: `csv.ts` で、後ろから入れる(`reverse`)のをやめて `push` にする・`unshift` を `push` にする、のどちらでも落ちる。確認して戻した
