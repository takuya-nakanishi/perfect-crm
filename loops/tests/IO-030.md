# IO-030

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。初期データのタスク(関連先が複数のテーブルに分かれ、空の行もある)を `related_object` で `count` し、各グループのラベルが `getMeta()` のテーブルの表示名(列名のままではない)、空のグループは `key: null`・「関連先なし」で末尾、件数は表を直接数えた値と一致することを確かめる(`docs/design/04` の aggregate の決まりと一致)。空のラベルを「未設定」にする・テーブル名を列名のまま返すの 2 通りで落ちることを確認済み
