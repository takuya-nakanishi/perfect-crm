# SET-048

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者で担当(user 型)を持つテーブルと、`defaults` が空のフォーム・`defaults` で担当を入れるフォームを作る。対照として画面から作ったレコードの担当は自分になることを確かめてから、ログインしたまま送っても・ログアウトして送っても、担当が空であること、`defaults` に入れた人なら担当になることを確かめ、保存された行でも同じことを確かめる。`submitForm` が担当にログイン中の利用者を入れるようにすると、`defaults` を足さないようにすると、それぞれ落ちることを確認済み
