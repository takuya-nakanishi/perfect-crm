# IO-010

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/filter.test.ts` に追加。お気に入りの「今日」と同じ形(`and` の中に「未完了」と `or`[期限 `lte $today` / 期限 `is_empty`])で、今日・過去・空の期限は真、明日の期限は偽、`or` に当たっても `and` の残り(完了済み)を満たさなければ偽になること、空の `and` は真・空の `or` は偽になることを確かめる。`lib/filter.ts` の `and` の `every` を `some` に、`or` の `some` を `every` にしたときにそれぞれ落ちることを確認済み
