# META-072

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追記。`system` でないテーブルを作り、ビューを 1 つ足し、レコードを 2 件入れてから `deleteObject` すると、`GET /meta` からテーブルとそのビューが消え、`table()` のレコードはそのまま残ることを確かめる。`restoreObject` で、テーブルの定義・ビュー(同じ ID・同じ定義)・レコードが削除前と同じに戻ることを確かめる。ビューは削除中の `GET /meta` には出ないので、「残る」ことは復元後に同じ ID で戻ることで確かめている
  - 期待値の確認: `engine.ts` の `deleteObject` でレコードを空にすると、また `schema.ts` の `deleteObject` でそのテーブルのビューを捨てると、それぞれ落ちることを確認して戻した
