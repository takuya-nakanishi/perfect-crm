# REC-066

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。取引先を `remove` すると `true` を返して行が 1 件減り `find` が `null` になること、消す前に取っておいた行を `restore` すると同じ id・同じ中身で戻り件数も元に戻ること、同じ行をもう 1 回 `restore` しても同じ id の行は 1 行のままで件数も増えないことを確かめる。期待値をわざと変えて落ちることを確認済み(戻した)
