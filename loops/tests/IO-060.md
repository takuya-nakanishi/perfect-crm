# IO-060

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` を新設。`parseCsv` が引用符の中の改行をセルの一部として残し、`""` を `"` 1 つに読むこと、先頭の BOM を見出しに残さないこと、CRLF を 1 つの改行として扱い(引用符の中の CRLF は文字のまま)、空行と空白・カンマだけの行を捨てることを確かめる。末尾の空行フィルタを外す変更と、`""` の読み替えを外す変更のそれぞれで落ちることを確認済み
