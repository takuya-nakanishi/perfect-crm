# META-053

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。お気に入りに入っているビューを `deleteView` すると、その応答にも `getMeta` にも無く、他のビューは変わらないこと。`restoreView` すると、同じ id で定義(並び・お気に入りを含む)がそっくり戻り、`getMeta().views` が消す前と一致することを確かめる
  - 期待値の確認: 戻すときに `position` を変える/ごみ箱のビューも `GET /meta` に出す、の 2 通りでそれぞれ落ちることを確認して戻した
