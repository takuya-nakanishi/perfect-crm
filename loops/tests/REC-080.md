# REC-080

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。fixture のテーブルには `scale` の列が無いので、`createObject` で `number`(scale 2・0・なし)と `percent`(scale 1)の列を持つテーブルを作って試す。数値の列に文字 `"12.34"`・`NaN`・`Infinity` を渡すと 400 で行が増えないこと(既存の `accounts.employees`・`opportunities.amount` でも同じ)、数値なら `scale` の桁で丸めて返り値と保存された行の両方に入り(12.3456→12.35、7.6→8、33.333→33.3)、`scale` の無い列は丸めないことを確かめる。期待値をわざと変えて落ちることを確認済み(戻した)
