# SET-046

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `mockClient.test.ts` に SET-046 を足した。`_gotcha` 付きの送信は例外なしで `id`・`created_at`・`updated_at` だけの応答になり、その id のレコードは 404、一覧にも出ず `submissions` も増えない。空の `_gotcha` の送信は通る(対照)

- [2026-09-22] 人が決めた: 設計どおり 200 で何もしない。モックの `submitForm` は `_gotcha` が埋まっていたら例外を投げず、`id`・`created_at`・`updated_at` だけの空の `RecordResponse` を返す(レコードも `submissions` も増やさない)。`docs/design/04` §10 の 3 に応答の形を追記し、表の SET-046 を「例外を投げず、レコードも submissions も増えない。空なら通る対照」に書き換えた

- [2026-09-22] 保留: 行の文が定義と食い違う。製品(モック)の側も設計と合っていない可能性がある
  - 設計: `docs/design/04-api.md` §10「Web フォーム」の 3 は **200 を返して何もしない**(bot に弾かれたと気づかせないため)
  - 索引の行: `docs/tests/settings.md` の SET-046 は **404 で何も作らない**
  - モック: `frontend/src/mocks/settings.ts` の `submitForm` は、コメントでは「成功に見せて何もしない」と書いているのに、実際には `ApiError(404, …)` を投げる。コメントと設計は 200、コードと索引の行は 404
  - 人が決めること: 200(設計どおり)にするなら、モックの `submitForm` を直す(`ApiClient.submitWebForm` はレコードを返す型なので、何も作らないときに何を返すかも決める)ことと、索引の行を「200 で何も作らない」に直す。404 のままにするなら、設計 04 §10 とモックのコメントを直す
  - どちらかに揃えば `状態:` を `未着手` に戻せば書ける(確かめる内容: `_gotcha` 付きの送信でレコードも `submissions` も増えず、`_gotcha` が空なら通る対照を置く)
