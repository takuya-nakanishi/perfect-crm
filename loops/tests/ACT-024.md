# ACT-024

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。新しく作った取引先責任者 Y について、`contact_id` が Y の done タスクを 2 件(関連先が取引先のもの・関連先なしのもの)、`contact_id` が Y の open を 1 件、別の責任者の done を 1 件、取引先だけを指す done を 1 件入れ、`timeline('contacts', Y)` が Y を `contact_id` で指す done 2 件だけを `kind: completion` で、`completed_at` の日付の新しい順に返すことを確かめる。`pointsTo` の relation の判定を外すと空になって落ちることを確認済み(製品のコードは戻した)
