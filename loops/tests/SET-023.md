# SET-023

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者でログインして `createMcpToken` を呼び、応答の `token.created_by` がログインした管理者の id、`last_used_at` が null で、`listMcpTokens` の同じトークンも同じ値であることを確かめる。`created_by` を別の値にする・`last_used_at` に時刻を入れる、の 2 通りでそれぞれ落ちることを確認済み
