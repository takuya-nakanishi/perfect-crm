# IO-066

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` に追記。選択肢 3 つの `multi_select` の項目で `coerce` を呼び、「営業、事務」が値の配列(JSON)`["sales","office"]` になること、前後の空白・カンマ区切り・値での指定も受けること、無いラベル(「経理」)が 1 つでもあれば「という選択肢はありません」で投げること、ラベルと値で同じ選択肢を重ねて書いても 1 つになることを確かめる
  - 期待値の確認: `csv.ts` で、重複除去(`new Set`)を外す・無い選択肢の throw を外す・区切りから「、」を外す、のどれでも落ちる。確認して戻した
  - 並びは入力順のまま確かめている。`docs/design/02` §「multi_select」の「定義順に揃える」は `coerce` では行っていないため、このテストでは定義順と入力順が同じ入力だけを使った
