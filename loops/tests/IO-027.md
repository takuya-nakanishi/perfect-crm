# IO-027

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。試しのテーブル(金額 `number`・確度 `percent`)に 6 行を入れ、`count` は条件に合う 4 行を金額が空の行も含めて数えること(`field` を指定しても同じ)、`sum` / `avg` は数値の 3 行だけで計算すること(avg は 1500 / 3 = 500)、数値の行が 0 件なら `sum` / `avg` とも 0 になること、`weight_field` で 1000 × 50 / 100 + 300 × 10 / 100 = 530 になり、金額が空の行は足さないことを確かめる。確度(重み)が空の行の扱いは定義(`docs/design/04` §集計)に無く、モックはそのまま足すが SQL 化の方針(`08` の `SUM(v * w / 100)`)では外れるため、確かめる対象に入れていない。重みを掛けない・avg の分母を全行にする・count で空を落とす・0 件の avg を 1 にするの 4 通りで落ちることを確認済み
