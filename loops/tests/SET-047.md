# SET-047

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者でテーブルとフォームを作り、作り直す前の鍵で送信が通ることを対照にして、`rotateWebFormKey` の応答の鍵が変わり、鍵のほか(id・名前・項目・送信の数など)は同じで、`listWebForms` も新しい鍵になることを確かめる。古い鍵は 404、新しい鍵は通る。もう一度作り直すと 1 つ前の鍵も最初の鍵も 404 になる。最後に、レコードが通った 3 件だけで、`submissions` も 3 であることを確かめる。`rotateKey` が鍵を変えないようにすると、`submitForm` が古い鍵でも受け付けるようにすると、それぞれ落ちることを確認済み
