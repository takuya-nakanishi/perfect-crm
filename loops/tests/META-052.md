# META-052

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。取引先のビューを 1 枚を残して `deleteView` で消し(ここまでは通る)、残った 1 枚を `deleteView` すると 400 で、そのビューが残り、他のテーブルのビューの数も変わらないことを確かめる
  - 期待値の確認: 最後の 1 枚の判定を外す/ごみ箱にあるビューも「残り」に数える、の 2 通りでそれぞれ落ちることを確認して戻した
