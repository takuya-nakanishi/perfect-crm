# META-090

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追記。商談テーブルの `close_date`(一覧の 5 列目・幅 130、並び `close_date asc` に使われている)を外して `updateObject` すると、`GET /meta` の一覧のビューから列と並びが外れ(ほかの列はそのまま)、同じ列名・同じ型で戻すと一覧のビューが外す前と完全に同じ(列は元の位置・幅、並びも元どおり。末尾に既定の幅で足されるのではない)になることを確かめる。保存してある定義に残っていることは、戻したときに元の位置・幅で現れることで確かめている(保存してある定義そのものは公開の操作から読めない)
  - 期待値の確認: `schema.ts` の `cleanView` で列を絞る部分を外すと落ちる(`expected [ …(6) ] to deeply equal [ …(5) ]`)。`updateObject` で外した項目を保存してある一覧の列・並びからも消すように変えると、戻したときに落ちる(`expected { …(6) } to deeply equal { …(6) }`)。どちらも確認して戻した
