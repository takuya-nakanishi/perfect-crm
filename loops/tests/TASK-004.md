# TASK-004

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`insert('tasks', { title })` で優先度・状況・担当を省くと、`priority` が `p4`、`status` が先頭の選択肢 `open`(未着手)、`assignee_id` が呼んだ人(Takuya でも Misaki でも、その人)になり、保存された行も同じであることを確かめる。送った値(p1・進行中・別の担当)は既定値より勝つことも見る。既定の優先度を p3 にする・担当の既定を null にする・状況の既定を 2 番目の選択肢にする変更で、それぞれ落ちることを確認済み
