# REC-063

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。商談を `insert` でフェーズ(提案・交渉・失注)を指定して作ると、確度が商談の `stage` の選択肢の定義にある既定値(50・85・0)になり、`find` で引き直しても同じこと、確度を同時に渡せば既定値で上書きしないこと(30、受注に 0 を渡しても 0)、フェーズを省けば先頭の選択肢(見込み)とその既定値になることを確かめる。既定値は種データの定義から引いて比べる。期待値をわざと変えて落ちることを確認済み(戻した)
