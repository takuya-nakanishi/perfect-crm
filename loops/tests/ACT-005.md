# ACT-005

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`insert`(activities)の内容で取引先と取引先責任者に言及すると、`mentions` が言及した順・重複なしの `["accounts:…","contacts:…"]` の JSON になり(返り値と保存された行で同じ)、内容に触れない `update` では変わらず、言及を差し替えれば差し替わり、言及を消す・内容を null にすると null になる(空の配列ではない)ことを確かめる。言及先の行とテーブルは `updated_at` も含めて元のままであることも見る。`mentions` を空でも `JSON.stringify` にしたとき、`update` で内容を写し直さないとき、言及先の `updated_at` を書き換えたときに、それぞれ落ちることを確認済み
