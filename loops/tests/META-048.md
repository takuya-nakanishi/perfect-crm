# META-048

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。取引先に一覧ビューを作るとき、名前が空・空白だけなら 400、無い項目を列・並び・条件(単独の条件と、and の中の or の奥)に指しても 400 で、弾いたときはビューが増えないことを確かめる。同じ本文で名前と項目が揃っていれば作れ、名前は前後の空白を落として保存されることも書いた
  - 期待値の確認: `checkViewInput` の名前の判定・並びの判定・列の判定を外す、または `or` の中を辿らないようにする、のどれでも落ちることを確認して戻した
