# SET-042

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者でログインして `createWebForm` を、名前が空(空白だけも)/ 無いテーブル / 項目 0 / readonly・`polymorphic`(関連先)・`drive_files` の項目を含む / `redirect_url` が `ftp:`・`javascript:` の各入力で呼び、それぞれ 400 でフォームが増えないことを確かめる。対照に、正しい入力と `http(s)` の `redirect_url` は通る。受け付けられない項目は、それを持つテーブルをメタデータから探す(テーブル名を書かない)。`checkForm` の readonly・polymorphic・drive_files・URL の検証を 1 つずつ外すと、それぞれ落ちることを確認済み
