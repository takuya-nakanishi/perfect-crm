# IO-084

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/richtext.test.ts` に追加。`extractMentions` が二重引用符と単引用符で同じレコードを 2 回言及しても 1 回だけ拾い(別のレコードは現れた順に拾う)、規則外の `data-id` や null は拾わないこと。`plainText` が段落・`<br>` を改行にしてタグを落とし、言及は `@名前` のまま残し、空の段落で改行が重ならないこと。`isEmptyHtml` が `<p></p>`・`<p><br></p>`・空文字・null を空とみなし、文字があれば空としないことを確かめる
  - 期待値の確認: 言及の重複を期待値に足すと落ちること、`<p></p>` を空でないとすると落ちること、`plainText` の期待値から `@` を外すと落ちることを 1 つずつ確認して戻した
