# META-027

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/tableDraft.test.ts` に追加。名前の無い新しい行(空白だけの名前も、選択肢型でも)が落ち、残りが元の順で並ぶこと、表示名の項目は `required: false` にしていても必須で送ること、`max_length` は文字型(`text`・`textarea`)だけに付き、数値・日付には付かないこと(数値の `scale` は残る)を確かめる。既にある項目は名前を消しても落とさないことも書いた
  - 期待値の確認: `f.isName` による必須化を外す・`TEXT_TYPES` の判定を外す・空行の除外をやめる、のどれでも落ちることを確認して戻した
