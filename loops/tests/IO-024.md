# IO-024

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。活動の本文(richtext、fixtures)で、`q` に `<strong>` の中の語(`帳票出力の遅さ` / `オフライン動作`)を渡すとその活動に当たること、タグの境目をまたぐ語(`確認。帳票出力` / `遅さが最優先`)も当たること、タグ名(`strong` / `<strong>` / `<li>` / `</p>`)には当たらないことを確かめる。`matchesText` の `plainText` を外すと落ちることを確認済み
