# ACT-003

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`insert`(activities)で種別と記録者を省くと 400 にならず、`type` に先頭の選択肢 `call`(電話)、`owner_id` に自分が入ること(返り値と保存された行の両方)、送った種別と記録者は既定値より勝つこと、`type: null` を明示すれば必須の検証で 400 になり行が増えないことを確かめる。`insert` の選択肢の既定値の行と、担当の既定値の行をそれぞれ外したときに落ちることを確認済み
