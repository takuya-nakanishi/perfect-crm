# IO-008

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/filter.test.ts` に追加。`$me` が `ctx.me` に解決すること(`in` の配列の中でも)、`ctx.me` が `null` なら誰とも一致しないこと、`$today-30` が月をまたいで 30 日前(2026-09-22 → 2026-08-23)になること、`$end_of_month` が 30 日の月・閏年の 2 月・12 月の末日になることを確かめる。期待値をずらしたときと、`lib/filter.ts` の `$me` を固定値にしたときに落ちることを確認済み
