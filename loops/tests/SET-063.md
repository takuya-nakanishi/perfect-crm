# SET-063

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加(SET-061・062 と同じ describe)。`listDriveFiles` で「テンプレ」が 3 件の雛形に部分一致し、ひらがな・半角カナ・前後や途中の空白・全角数字でも同じ結果になること、当たらなければ空、空(空白だけも)なら全 8 件が出ることを確かめる。20 件の上限は、`createDocument` でファイルを 28 件にしてから `listFiles` の空の検索と部分一致(23 件一致)がどちらも 20 件で切れることで見る(API 経由だと遅延で 5 秒を超えるため drive.ts を直に呼ぶ)。上限を外す・正規化を外す、の 2 通りの壊し方でそれぞれ落ちることを確認済み
