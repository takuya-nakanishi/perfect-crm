# SET-005

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。セッションが無い状態で `getMeta` / `listRecords` がどちらも 401 になり、ログインすると同じ呼び出しが通り、ログアウトすると再び 401 になることを確かめる。`getMeta` から `requireUser()` を外すと落ちることを確認済み(確認後に戻した)
