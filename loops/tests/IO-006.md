# IO-006

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/filter.test.ts` に追加。`is_empty` は NULL・列が無い行・空文字・`[]` で真、`is_not_empty` はその逆であること、`0`・`false`・中身のある配列は空と見なさないことを確かめる。`lib/filter.ts` の `raw === '[]'` を外したときと、`isEmpty` を偽値判定(`!v`)に変えたときに落ちることを確認済み
