# SET-022

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者でログインし、`createMcpToken` の名前が空・空白だけのとき 400 でトークンが増えないことを見る。続けて発行したトークンを `revokeMcpToken` で失効させると一覧から消え、失効済みの id・無い id では 404 で一覧が変わらないことを確かめる。空の名前を許す・404 を返さない・失効しても消さない、の 3 通りでそれぞれ落ちることを確認済み
