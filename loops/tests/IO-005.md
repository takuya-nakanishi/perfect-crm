# IO-005

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/filter.test.ts` に追加。`contains` は大文字小文字を区別せず部分一致で真・含まなければ偽、文字でない列(数値・真偽・NULL・列が無い行)は文字に直して比べず偽であることを確かめる。`lib/filter.ts` の小文字化を外したときと、`String(cell)` で文字に直して比べるように変えたときに落ちることを確認済み
