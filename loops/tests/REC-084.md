# REC-084

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に REC-084 を書いた。商談の行の必須の列(商談名・取引先)を直接空にしておき、金額と完了予定日だけを渡す `update` が通って空の必須の列はそのままであること、本文に必須の列を空で渡すと 400 で行が変わらないこと、商談名だけを埋める更新は取引先が空のままでも通ることを確かめた。`update` の `validate` に `keys` を渡さない(全列を検証する)ように壊すと、また `validate` の「本文に無い列は飛ばす」を消すと落ちることを確かめてから戻した
