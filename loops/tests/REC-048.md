# REC-048

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/records.test.ts` に追加。`isClosed` は select の選択肢の `kind` が won / lost / done なら真、open・kind 無し・空・選択肢に無い値・select 以外の列に同じ文字が入っているだけなら偽であることを確かめる。`isEmptyValue` は polymorphic を ID 列で見る(テーブル名の列や key の列に値があっても ID が空なら空)こと、ほかの型は key の列で null・undefined・空文字を空とし 0・false は空としないことを確かめる。`done` の判定を外す・polymorphic をテーブル名の列で見る・空文字の判定を外すとそれぞれ落ちることを確認済み(製品のコードは戻した)。`f.type !== 'select'` の判定を外しても落ちないが、select 以外の列は選択肢を持たず `optionOf` が常に undefined になるため、振る舞いに差が出ない
