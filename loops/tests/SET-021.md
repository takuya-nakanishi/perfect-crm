# SET-021

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者でログインして `createMcpToken` を呼び、`secret` が `wks_` + 英数字 40 文字、`token.prefix` がその先頭 8 文字であることを確かめる。続けて `listMcpTokens` で、発行したトークンが一覧に入り、`secret` の項目も全文の文字列も含まれないことを見る。全文を 39 文字にする・`prefix` を 7 文字にする・保存するトークンに `secret` を持たせる、の 3 通りでそれぞれ落ちることを確認済み
