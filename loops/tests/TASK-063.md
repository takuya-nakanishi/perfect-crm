# TASK-063

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。タスクの labels に配列でない文字(`sales`・`"sales"`・`sales,follow_up`)を `update` すると 400 になり、保存済みの `["sales"]` が変わらないことを確かめる。製品側で「配列の形か」の検査を外すと落ちる(null のまま保存される)ことを確認済み(戻してある)。なお `[` で始まる壊れた JSON(例 `[sales`)は、いまの製品では 400 にならず null で保存される。このケースの範囲外なので扱っていない
