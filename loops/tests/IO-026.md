# IO-026

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。取引先を「の」で探すと、当たる 13 件のうち名前に「ノ」を含む 3 件が先に出て(表の順では名前以外で当たる行が前にある)、6 件で切れること、5 つのテーブルとも出る件数が `query` の `total` と 6 の小さい方になること(タスクは 32 件当たって 6 件)、サイドバーに出していない活動(`in_sidebar: false`)も対象で、「見積」では件名で当たる行が本文だけで当たる行より先に出ることを確かめる。名前での並べ替えを外す・上限を 7 にする・サイドバーのテーブルに絞るの 3 通りで落ちることを確認済み
