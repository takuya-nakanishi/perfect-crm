# ACT-044

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。種データ(`resetTables` 直後)の活動のうち内容に言及があるものを全部取り、`mentions` 列が内容の `data-id` を順に並べた JSON と一致すること(gen-fixtures が写している)、言及先ごとの `timeline` にその活動が 1 件だけ、関連先と別なら `kind: mention`・同じなら `activity` で出て、`related` が本来の関連先(名前付き)であることを確かめる。言及として出る種データが 1 件以上あることも確かめる。種データの `mentions` を null にする・`timeline` の言及の判定を外す、の 2 通りでそれぞれ落ちることを確認済み(どちらも戻した)
