# ACT-027

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。新しい取引先 A と、画面から足したテーブル `projects`(T)のレコード R を作り、関連先を R にして本文で A に言及した活動を入れる。`timeline('accounts', A)` にその活動が `kind: mention`・`related: { object: 'projects', id: R, name: R の名前 }` で出ることを確かめ、`deleteObject('projects')` のあと(T は `getMeta` から消え、活動の `related_object` / `related_id` は保たれる)と `restoreObject('projects')` のあとで、時系列が削除前と同じであることを確かめる。`timeline` の `nameOf` を「いま使えるテーブルだけ名前を引く」に変えると `name` が空になって落ち、「関連先のテーブルが削除済みなら飛ばす」に変えても落ちることを確認済み(製品のコードは戻した)
