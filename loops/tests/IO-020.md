# IO-020

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`query` で商談をフェーズ(選択肢)で昇順に並べると、見込み → … → 失注の定義順に束になり(値の文字順 hearing, lead, lost, … ではない)、降順は失注 → 見込みの逆順になること、タスクを優先度で並べると P1 → P4、降順で P4 → P1 になることを確かめる。`query` の `keyOf` で選択肢を定義順の位置ではなく値そのままで比べる変更にすると落ちることを確認済み。優先度は値の文字順と定義順が一致するので、文字順との区別はフェーズの側で担保している
