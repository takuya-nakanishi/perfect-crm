# IO-011

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/filter.test.ts` に追加。数値は数値順(9 が 10 より前)、文字(カナ)は文字順で、NULL は昇順・降順どちらでも末尾に来ること、1 つめのキー(段階・昇順)が同点のとき 2 つめのキー(金額・降順)で割り、そこでも NULL が末尾になること、全キーが同点なら 0 を返すことを確かめる。`lib/filter.ts` の `makeComparator` で、降順のとき NULL を先頭にする・数値を文字として比べる・同点で次のキーへ進まない・降順を無視する、の各変更で落ちることを確認済み
