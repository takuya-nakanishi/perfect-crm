# ACT-043

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。新しい取引先 Z を作り、関連先を Z にして本文でも Z に 2 回言及した活動を 1 件入れる。`mentions` 列に Z が写ったうえで、`timeline('accounts', Z)` にその活動が `kind: activity` で 1 件だけ出て、`related` が Z(名前付き)であることを確かめる。`kind` を言及優先で `mention` にする・直接と言及の両方なら 2 件積む、の 2 通りに製品のコードを変えるとそれぞれ落ちることを確認済み(製品のコードは戻した)
