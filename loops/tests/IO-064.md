# IO-064

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` に追記。`coerce` に項目の定義を直接渡し、数値(全角 `１２３`・`1,200,000`・`¥1,200`・`￥３，４００`・`5,000円`)、日付(`2026/9/30`・`2026年9月30日`・全角の年月日。1 桁の月日は 0 で埋める)、選択肢(ラベル「高い」でも値 `cold` でも値を返す)、利用者(名前でもメールでも、メールは大文字でも ID を返す)、チェック(「はい / true / TRUE / 1 / ○」は true、それ以外は false、空は null)を確かめる。読めない値(「千円」・`9月30日`・無い選択肢・いない利用者)は理由付きで投げることも確かめる
  - 期待値の確認: `csv.ts` で、数値の除去から `¥`・`,`・`円` を外す・日付の区切りを `-` だけにする・選択肢をラベルで引かない・利用者をメールで引かない・`TRUE_WORDS` から `'1'` を外す、のどれでも落ちる。確認して戻した
