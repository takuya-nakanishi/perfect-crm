# META-056

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/viewModel.test.ts` に追記。選択肢の項目を持たないテーブル(表示名を readonly・複数行の文字の後ろに置く)で、カンバン → null(一覧の base を渡しても null)、base 無しの一覧 → readonly と textarea・richtext を除いた先頭 6 項目(表示名を含み、幅は型ごとの既定。表示名は 280)、base が一覧 → base の列をそのまま(候補外・readonly・幅の無い列も含めて)写し、フィルタと並びも受け継ぐことを確かめる
  - 注: 「表示名を含む」は、表示名が候補の先頭 6 項目に入っているときに限る(製品は表示名を先頭へ足す処理を持たない)。テストでは表示名が候補の 1 番目になる並びにしている
  - 期待値の確認: 6 → 5 項目にする/readonly を除かない/base の列を写さない/選択肢が無くても一覧を返す/複数行の文字を除かない(製品側を一時的に変更)、の 5 通りでそれぞれ落ちることを確認して戻した
