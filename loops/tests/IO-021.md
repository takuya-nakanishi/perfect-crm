# IO-021

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`query` で商談を取引先(参照)で昇順に並べると取引先名の順(`localeCompare` の `ja`)に束になり、ID の順とは違うこと、返した `references` の表示名で見ても順に並ぶこと、降順はその逆になることを確かめる。担当者(利用者)で並べると名前の順(Misaki → Takuya)になり、ID の順(Takuya が先)とは逆になること、降順はその逆を確かめる。`query` の `keyOf` で参照・利用者を表示名ではなく ID のまま比べる変更にすると落ちることを確認済み
