# IO-046

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/dates.test.ts` に追加。今日を 2026-09-22 で渡し、`$today` / `$today+7` / `$today-30` / `$today+0`、月・年をまたぐ `$today+10` / `$today-1`、`$start_of_month` / `$end_of_month`(2 月・うるう年・12 月末)を解決すること、`$yesterday`・`$today+`・`$today*2`・末尾に空白・`$me`・ただの日付・空文字は null になることを確かめる。製品の符号を無視する、正規表現の末尾の `$` を外す、知らない値で null でなく値を返す、月末の計算を翌月 1 日にする、の各変更で落ちることを確認済み(製品は元に戻した)
