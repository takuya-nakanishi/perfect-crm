# REC-065

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。取引先を `update` で名前だけ変えると、返り値も `find` で引き直した行も「元の行 + 名前 + `updated_at`(業務ルールで今になる)」と完全に一致し(id・created_at を含む他の列はそのまま)、他の行も変わらないこと、無い ID では `null` を返してどの行も変えないことを確かめる。時計は `vi.useFakeTimers` で固定。期待値をわざと変えて落ちることを確認済み(戻した)
