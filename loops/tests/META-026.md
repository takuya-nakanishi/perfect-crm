# META-026

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/tableDraft.test.ts` に追加。不備の無い下書きが 0 件になること、予約語(`id`)・選択肢の無い選択肢型(空白だけの選択肢も無いとみなす)・参照先の無い参照型が、その行に 1 件ずつ(計 3 件)出ることを確かめる。名前の無い新しい行は、選択肢型でも、既にある列名と重なっていても数えない
  - 期待値の確認: 予約語の集合から `id` を外す・空行の除外をやめる・参照先の判定を外す、のどれでも落ちることを確認して戻した
  - 注: 列名の重複は、重なった 2 行の**どちらにも**出る(`validateDraft` の作り。1 組で 2 件)。行の文「それぞれ 1 件ずつ」は「行ごとに最初の不備を 1 件」と読み、重複は 2 行とも数えることをテストに書いた。1 組を 1 件にしたいなら製品側の変更になる
  - 注: `newField` の自動の列名は渡した一覧しか見ないため、同じ一覧から続けて作ると `field_1` が重なる。テストでは列名を明示した
