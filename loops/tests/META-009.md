# META-009

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`select`・`multi_select` の両方で、`options` 無し・空の配列・名前が空白だけの選択肢(捨てられて無いのと同じ)・値が重なる選択肢・9 色の外の色のどれでも `createObject` が 400 を返し、テーブルが作られないことを確かめる。9 色を 1 つずつ使った別々の値の選択肢なら作れて、色がそのまま残ることも確かめる。`mocks/schema.ts` の 3 つの検査(選択肢の数・値の重なり・色)をそれぞれ外すと落ちることを確認済み
