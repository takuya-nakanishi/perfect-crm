# IO-062

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` に追記。取引先へ `mapping` を省いて `importCsv` し、見出しの推測を確かめる。項目名は正規化して比べる(前後の空白 `' 取引先名 '`、全角と全角空白 `ＷＥＢ　サイト` → `website`、ひらがな `ふりがな` → `name_kana`)、列名は大文字小文字を問わない(`PHONE` → `phone`)。後から同じ項目を指す列(`電話`・`industry`)は `null`、読み取り専用(`created_at`)と知らない見出しも `null`。作った行は先の列の値を持つ
  - 期待値の確認: `csv.ts` の `!Object.values(mapping).includes(chosen)` を外すと落ち、`guessField` の `normalizeText` による比較を素の一致にしても落ちる。確認して戻した
