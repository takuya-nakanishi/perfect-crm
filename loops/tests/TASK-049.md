# TASK-049

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。9/15 に作った繰り返しタスクを 9/22 に done にすると、元の行は created_at=9/15・completed_at=9/22 のまま、次回は新しい id で created_at・updated_at が完了した今、completed_at が null、status が open になることを確かめる。検証が効くことは、写す値(取引先責任者)の参照先を消してから done にすると 400 になり、次回ができず行数も変わらないことで確かめる。製品側で次回を `insert` を通さず元の行を写して直接入れると落ちること、作成日時などは入れるが検証を通さないと落ちることを確認済み(戻してある)
