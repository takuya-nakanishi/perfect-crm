# SET-004

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。管理者でない利用者(fixtures の Misaki)でログインし、`createView` / `updateView` / `deleteView` がどれも例外を投げず、ビューが定義に入り・名前が変わり・消えて元の並びに戻ることを確かめる。`deleteView` に `requireAdmin` を足すと落ちることを確認済み(確認後に戻した)
