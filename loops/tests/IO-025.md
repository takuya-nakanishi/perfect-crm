# IO-025

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。商談(3 件のページ。参照 2 つ + 担当者)、活動(関連先に取引先と商談が混ざる + 担当者)、タスク(参照・関連先が空の行を含む + 担当者)で、`references` のテーブルごとの ID が、返した行が指す ID の集合とちょうど一致すること(ページに無い行の参照先は入らない)、表示名が参照先の name_field と同じことを確かめる。`collectReferences` の polymorphic の分岐を外す・全件の行から集めるの 2 通りで落ちることを確認済み
