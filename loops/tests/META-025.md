# META-025

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/tableDraft.test.ts` を新しく作って追加。`newField` / `autoFieldKey` で、`Lead Source` → `lead_source`、和文 `見積番号` → `field_1`、2 つめの和文 → `field_2` になることを確かめる。既にある列名(`lead_source`・`name`)と予約語(`id`)は `_1` を付けて避け、和文は使われていない次の番号(`field_3`)になる。いちど付けた連番は名前を打ち直しても変わらないことも合わせて確かめる
  - 期待値を `lead_source`(避けない)や別の番号に変えると落ちることを確認済み
  - 注: 自分の列名がすでに連番(`field_N`)で空いていれば、英字の名前が既存と衝突してもその連番を保つ(`tableDraft.ts` の「連番は、いちど付けたらそのまま」)。避ける確認は列名の無い欄で行った
