# IO-069

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/csv.test.ts` に追記。タスクへ「件名・関連先・関連先のテーブル・関連先のID・資料・完了日時・作成日時」の CSV を取り込み、見出しからの推測でも、明示の mapping(`related`・`related_object`・`related_id`・`documents`・`completed_at`・`created_at`)でも、件名以外の列が null になること、作った行がそれらの値を持たない(作成日時も CSV の値にならない)ことを確かめる
  - 期待値の確認: `csv.ts` の `importable` から `!f.readonly`・`polymorphic` の除外・`drive_files` の除外をそれぞれ外すと落ちる。確認して戻した
