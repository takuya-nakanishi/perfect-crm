# IO-009

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/filter.test.ts` に追加。複数選択(`'["vip","hot"]'`)に対して `in` / `eq` がどれかを含めば真、`not_in` / `ne` がどれも含まなければ真(1 つでも含めば偽)になること、空の配列は何も含まないこと、壊れた JSON(`'["vip","hot"'`)は配列と見なさずただの文字として比べることを確かめる。`lib/filter.ts` の `some` を `every` にしたときと、壊れた JSON を補って配列にしたときに落ちることを確認済み
