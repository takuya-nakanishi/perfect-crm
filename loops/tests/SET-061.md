# SET-061

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加(`lib/drive.test.ts` と同じ名前のファイルを作ると表の整合チェックがファイル名で混同するため、こちらに置いた)。表示名の項目が `name` でないテーブル(tasks は `title`)で、先に「参照」で 1 件付けたレコードに `createDriveDocument` を呼び、付いたものが既存の後ろに足されること、名前がレコードの表示名で MIME がドキュメントであること、作る前の `listDriveFiles` には無く、作った後は名前の部分一致と空の検索で見つかること、保存された行でも同じであることを確かめる。先頭に足す・名前を `name` から取る・一覧に足さない・MIME を PDF にする、の 4 通りの壊し方でそれぞれ落ちることを確認済み
