# REC-081

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。初めからあるテーブルの日時の列は読み取り専用なので、`createObject` で書ける `date` と `datetime` の列を持つテーブルを作って試す。日付の列に `2026/09/30`・`2026-13-01`・時刻付き・数値、日時の列に `2026/09/30 10:00`・日付だけ・13 月を渡すと 400 で行が増えないこと(既存の `opportunities.close_date` でも同じ)、`2026-09-30` と ISO 8601 の日時(`+09:00`・`Z`)は通ってそのまま保存されることを確かめる。期待値をわざと変えて落ちることを確認済み(戻した)
