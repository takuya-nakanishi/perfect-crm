# ACT-022

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。新しく作った取引先 X を関連先にした活動を、作った順(`created_at`)と日付(`occurred_on`)の順をわざとずらして 4 件入れ、別の取引先の活動も 1 件入れる。`timeline('accounts', X)` が X の 4 件だけを日付の新しい順、同じ日は `created_at` の新しい順で返し、全件 `kind: activity`・`related` が X(名前つき)であることを確かめる。同じ日の並びを古い順に変えたとき、`kind` を `mention` にしたときに、それぞれ落ちることを確認済み
