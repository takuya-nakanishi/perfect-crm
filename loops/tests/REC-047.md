# REC-047

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/records.test.ts` を新設。relation(テーブルは `target`、ID は列の値)・user(テーブルは `users`)・polymorphic(`columns.object` と `columns.id` の 2 列から。`key` の列は見ない)がそれぞれ `{ object, ref }` を返すこと、`references` に表示名が無いときは `{ id, name: '…' }` で成立すること、値が空・テーブル名が空・参照型でない列では `null` を返すことを確かめる。`refFor` の既定名を `''` に、user のテーブルを別名に、polymorphic の ID を `key` の列から読むように変えるとそれぞれ落ちることを確認済み(製品のコードは戻した)
