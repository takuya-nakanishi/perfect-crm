# IO-081

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/format.test.ts` に追加。`formatNumber(12.54)` が `13`、`scale: 2` で `12.54`、`scale: 0` で `13`、`formatPercent(12.54)` が `12.5%`、`scale: 2` で `12.54%`、`scale: 0` で `13%`(0 を指定なしと見なさない)になることを確かめる
  - 期待値の確認: `formatPercent(12.54, 0)` の期待値を `12.5%` に変えると落ちることを確認して戻した
