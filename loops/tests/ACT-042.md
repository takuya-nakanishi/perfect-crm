# ACT-042

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。新しい取引先 Z と、別の取引先・取引先責任者を作り、それぞれを関連先にして本文で Z に言及した活動 2 件と、Z に言及しない活動 1 件を入れる。`timeline('accounts', Z)` に言及した 2 件だけが日付の新しい順に `kind: mention` で出て、`related` がその活動の本来の関連先(`contacts` / `accounts` の ID と名前)であることを確かめる。`kind` を常に `activity` にする・`related.name` を空にする・言及だけの活動を飛ばす、の 3 通りに製品のコードを変えるとそれぞれ落ちることを確認済み(製品のコードは戻した)
