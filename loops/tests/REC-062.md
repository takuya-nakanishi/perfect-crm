# REC-062

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に REC-062 を書いた。商談(必須の `stage` は先頭・`owner_id` は自分)とタスク(必須の `status` は先頭・`assignee_id` は自分)で、`created_at` と `updated_at` が今(時計を止めて比べる)。`bogus` 入りの `insert` は 400 で行が増えず、`update` も 400 で表がそのまま。`readonly` の `completed_at` は `insert` / `update` の本文にあっても通り、値が保たれる。`insert` の `rejectUnknownColumns` を外すと落ち、`user` 型の既定を null にしても落ちることを確かめた

- [2026-09-22] 人が決めた: 定義に無い列が本文にあれば 400(`定義に無い列です: <列名>`。`docs/design/04` §11)。`insert` と `update` の入口で `rejectUnknownColumns` が弾くようにモックを直し、表の REC-062 も「400 で何も作らない・行は変わらない」に書き換えた。`updated_at` は `applyRules` が毎回今にする(`next.updated_at = now`)ので、作成時も今になる

- [2026-09-22] 保留: テストは書いていない。`frontend/src/mocks/engine.ts` の `insert` は既定値(必須の選択肢は先頭・`user` 型は自分・`created_at` は今)を埋めたあと `Object.assign(row, effective)` で本文の全キーを行に写すため、定義に無い列(例 `bogus`)も行に入る。行の後半を期待どおりに書くとテストが落ち、通すには製品(モック)を直す必要がある。`docs/design/04` の `POST /objects/{object}/records` も「本文は列名 → 値」とだけあり、未知の列を捨てるのか 400 にするのかが決まっていない。人がすること: 未知の列の扱い(黙って捨てる / 400)を `docs/design/04` で決め、モックを揃えてから `状態:` を `未着手` に戻す。なお `updated_at` が作成時に今になるのは `applyRules` 側の確認が要る(本件では未確認)
