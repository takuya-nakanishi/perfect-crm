# IO-083

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/lib/richtext.test.ts` に追加。`sanitizeHtml` が規則どおりの言及 `<span data-type="mention" data-id="accounts:<uuid>" data-label="…">` を `data-type`・`data-id`・`data-label`・`role="link"`・`tabindex="0"` だけを付けて残し(`class`・`onclick` は落ち、中の要素は平らにして文字だけ)、`data-id` が規則外(UUID でない・テーブル名が大文字・UUID が大文字・テーブル名が無い・後ろに余分な文字・属性が無い)なら `<span>` を外して中身の文字だけ残すことを確かめる
  - 期待値の確認: 言及の期待値から `role="link"` を外すと落ちること、規則外の期待値を `<span>` 付きにすると落ちることを確認して戻した
