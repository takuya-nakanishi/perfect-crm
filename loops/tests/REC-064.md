# REC-064

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。商談を `update` でフェーズだけ変えると(提案・交渉・失注)、手で変えていた確度も商談の `stage` の選択肢の定義にある既定値になり、`updated_at` がその時刻に進むこと(`created_at` は変わらない。`find` で引き直しても同じ)、確度を同時に渡せば既定値で上書きしないこと(30、0 も尊重)、フェーズを変えない更新では確度がそのままで `updated_at` だけ進むことを確かめる。時計は `vi.useFakeTimers` で固定。期待値をわざと変えて落ちることを確認済み(戻した)
