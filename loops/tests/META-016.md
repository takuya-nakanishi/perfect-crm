# META-016

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。タスクの現在の定義から画面と同じ全量の本文(readonly の列は含めない)を作り、状況(`status`)の選択肢から `done`・`open` を 1 つずつ消して `updateObject` に送ると 400 になり、選択肢もテーブル名も元のまま残ることを確かめる。完了の仕組みが使わない選択肢(`in_progress`)なら同じ形の本文で消せることも確かめる。
  - `mocks/schema.ts` の「`done` / `open` の選択肢は消せない」検査を空にすると落ちることを確認済み
