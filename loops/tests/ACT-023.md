# ACT-023

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。新しく作った取引先 X を関連先にしたタスクを、done(`completed_at` と詳細あり)・done(詳細なし)・open の 3 件、別の取引先を関連先にした done を 1 件入れる。作った日時(10/01)と `completed_at`(9/15・9/18、タイムゾーンでずれないよう昼の時刻)をずらし、`timeline('accounts', X)` が X の done 2 件だけを `kind: completion` で返し、`date` が `completed_at` の日付、`body` が詳細を `<p>` の段落にしてエスケープしたもの(詳細なしは null)であることを確かめる。`date` を作った日に、件数を 3 件に、`body` を段落なしに変えたときに、それぞれ落ちることを確認済み
