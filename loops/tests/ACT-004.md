# ACT-004

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。`insert`(activities)の内容に `<script>`・`onclick` / `onmouseover`・`<img onerror>`・`<iframe>`・`javascript:` のリンク・`style` を混ぜて送ると、返り値と保存された行の `body` が同じ洗浄済みの HTML になり、許した要素(`p`・`strong`・`a`)と文字だけが残る(スクリプトの中身は文字として残り、`javascript:` のリンクは href が落ちた `<a>` になる)ことを確かめる。`applyRules` の `sanitizeHtml` の呼び出しを外したとき、`sanitizeHtml` の href の検査を外したときに、それぞれ落ちることを確認済み。happy-dom は `<iframe src>` の URL を実際に取りに行くので、外部を指す src は使わない
