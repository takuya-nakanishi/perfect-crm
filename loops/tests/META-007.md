# META-007

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。2 番目の項目の列名が `id`・`created_at`・`updated_at` のテーブルで `createObject` が 400 を返し、テーブルが作られないこと、同じ列名の項目が 2 つ(型が違っても)あるテーブルも 400 で作られないことを確かめる。予約語を含むだけの列名(`external_id`・`created_at_origin`)なら作れることも確かめる。予約列名の検査を外すと落ち、重複の検査を外しても落ちることを確認済み
