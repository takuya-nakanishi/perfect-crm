# SET-002

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` を新設。管理者でない利用者(fixtures の Misaki)でログインし、`createObject` / `updateObject` / `deleteObject` / `reorderObjects` がそれぞれ 403 を返し、テーブルの定義が変わらないことを確かめる。対照として管理者なら `createObject` と `reorderObjects` が通ることも見る。`requireAdmin` を無効にすると落ち、`reorderObjects` だけ `requireAdmin` を外しても落ちることを確認済み
