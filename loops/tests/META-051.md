# META-051

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/engine.test.ts` に追加。取引先に `pin` 付きのビューを 2 つ作り、1 つ目を名前だけ変えて `updateView` しても `pin.position` はそのまま(本文の `pin.position` を 999 にしても動かない)。`pin` を外して保存すると `pin` が消え、元の位置を渡して付け直すと、元の位置ではなくお気に入り全体の末尾 +1 になり、もう 1 つのお気に入りの位置は変わらないことを確かめる
  - 期待値の確認: `updateView` が既にある `pin.position` を使わず毎回末尾に振り直す/本文の `pin.position` をそのまま使う、の 2 通りでそれぞれ落ちることを確認して戻した
