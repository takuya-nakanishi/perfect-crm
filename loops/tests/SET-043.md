# SET-043

- 状態: 完了(2026-09-22)

## 試行の記録

- [2026-09-22] 完了: `frontend/src/mocks/mockClient.test.ts` に追加。必須が文字の項目だけで、ほかに文字の項目を 3 つ持つテーブルをメタデータから探し(テーブル名を書かない)、1 つを `fields`、1 つを `fields` に無い列、1 つを `defaults` にしたフォームを管理者で作る。ログアウトして `submitWebForm` に、`fields` の列・`fields` に無い列・テーブルに無い列を送り、できたレコードで受けた列は入り、捨てる列は空、`defaults` は足されていることを確かめる。ログインし直して `getRecord` でレコードがあること、`listWebForms` で `submissions` が 0 → 1、`last_submitted_at` が送信の前後の時刻の間に入ることを確かめる。`submitForm` の `defaults` を足す処理・`fields` で絞る処理・`submissions += 1`・`last_submitted_at` の更新を 1 つずつ外すと、それぞれ落ちることを確認済み
